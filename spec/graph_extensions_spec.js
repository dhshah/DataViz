
describe('graph extensions', function() {
  var graph;
  beforeEach(inject(function($q) {
    graph = new Graph($q);
  }));

  it('should mark visted correctly', function() {
    // TODO(dhshah) actual tests
    expect(true).toBe(true);
  });

});
